componentDidMount() {
    axios
      .get(
        "https://restcountries.com/v2/all?fields=name,capital,flags,languages,population"
      )
      .then((res) => {
        this.setState({ data: res.data });
        console.log(this.state.data);
      });
  }

  home component , we do routing here, it not do get itself,so it's f component,landing page

  countries list:class component, we need data

  countries card:it's a small place holder,f component

  coountry single:it should have more info that countries list, we need all tha data of one country, so we need to fetch again the api.We don't pass from any other file.Class component.

  You can make sure as a individual component